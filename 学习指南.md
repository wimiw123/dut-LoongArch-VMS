# MyCPU (LoongArch) 模拟器学习指南 🚀

欢迎来到这个精简版的 LoongArch CPU 模拟器项目！如果你是一个编程或者计算机体系结构的小白，不要害怕，这份指南将带你一步步拆解并搞懂这套代码。

在这份代码中，我已经为你把 `include` 和 `src` 下的所有文件逐行加入了极其详细、生动幽默的中文注释。

## 🗺️ 第一步：这个项目是干什么的？

这是一个**用软件（C++代码）来模仿硬件CPU行为**的程序。
简单来说，它就像一个虚拟机，能够在你的电脑里虚拟出一个“龙芯（LoongArch）”处理器，以及配套的内存、定时器、串口等外设，专门用来运行龙芯编译出来的二进制或汇编机器码。

## 🔍 第二步：正确的阅读顺序（关键！）

<!-- 不要一上来就死磕最复杂的 `CPU.cpp`，请按照以下顺序“打怪升级” -->

### 1. 认识环境与基础设备（先看头文件，再看实现）
这部分最简单，它们是外部的物理零件。
- `include/PlatformConfig.h`：这是整个系统的大地图，告诉你内存多大、组件都在哪个物理地址（门牌号）上。
- `include/Device.h`：所有硬件设备的“老祖宗接口”。理解了读写 32 位数据（`read32` / `write32`）的概念，后面就全通了。
- `include/Memory.h` 和 `src/Memory.cpp`：它是最基础的一块内存条。看看它是怎么用简单的 C++ 数组 `std::vector` 模拟真实的物理字节存放的（小端序存取是重点）。
- `include/Uart.h` 和 `src/Uart.cpp`：简陋的物理输出串口，看看程序是怎么把一个数字变成字母印在屏幕上的。

### 2. 认识系统大管家（总线）
- `include/Bus.h` 和 `src/Bus.cpp`：这是连接 CPU 和所有外部零件的“十字路口交警”。当 CPU 这边大喊“我要往某个地址写数据”时，总线负责判断并把数据转交给上面看过的 Uart 或者 Memory 或者其他外设。

### 3. 解锁外挂测试组件（定时器与黑盒测试）
- `include/TestDevice.h` 和 `src/TestDevice.cpp`：这是为了方便测试程序有没有跑完而特意做的“伪装外设”。
- `include/Timer.h` 和 `src/Timer.cpp`：一个定时器硬件。这里面包含了一个很关键的概念——**中断（Interrupt）**，看它是怎么通过定期算步数来强行打断 CPU 干活的。

### 4. 拆解指令与译码器（核心准备）
- `include/decoder.h`：这里面是一大堆纯数学位运算（与、或、移位）。它的作用是拿一把精确的手术刀，把 CPU 取回来的一长串 32位 二进制数字，一片片切开，提取出“你到底是什么操作”、“你的参数是什么”。**一定要看懂 `extract_bits` 和 `sign_extend` 是干什么用的。**

### 5. 挑战终极大魔王：CPU核心
- `include/CPU.h`：先看 CPU 包含什么东西（32 个通用寄存器阵列、程序计数器 PC、几个关键的特权 CSR 寄存器状态标志）。
- `src/CPU.cpp`：这里的 `step()` 函数是整个模拟器最核心的心跳！
  仔细研读里面的流水线三步曲：
  1. **取指 (Fetch)**：利用 PC 地址去总线要那段 32位的密码指令。
  2. **译码 (Decode)**：利用 `decoder.h` 翻译出密码指令的意图。
  3. **执行 (Execute)**：最长的那一大串 `if-else` 和 `switch`，就是在穷举所有的可能指令动作（加法、跳转、存取内存等），然后根据操作要求干涉修改它自己兜里（m_regs 数组）的数据或内存的数据。

### 6. 外围辅助系统
- `include/ProgramLoader.h` & `src/ProgramLoader.cpp`：搞懂了 CPU 是怎么运行的，你就会好奇它运行的代码最开始是怎么被灌进内存池的？这就是在干装货搬运工的活。
- `include/SimulatorRunner.h` & `src/SimulatorRunner.cpp` 以及 `simulator_main.cpp`：这是整个系统开机上电的入口（main 函数）。它把以上的 内存、总线、CPU 全部串联实例化拼接出来，灌入程序文本，然后写个大大的死循环踹着 CPU 也就是反复调用 `cpu.step()` 一步步跑下去直到程序喊停。

## 💡 第三步：边读边动手的建议

1. **利用好代码里的中文注释**：现在的源代码里包含了通俗的比喻（例如把寄存器比作兜里的钱，总线比作交警），遇到看不懂的位运算，先看汉字注释去理解宏观意图。
2. **使用断点调试**：使用 VSCode 或者其他 IDE，将断点打在 `CPU.cpp` 的 `cpu.step()` 里面。然后一步一步往下跑，死死盯着 `curr_pc`（当前位置）的值是怎么跳动的，也看看 `m_regs`（寄存器大金库数组）里面的数值是怎么随着你执行的指令被修改的。
3. **结合架构手册一起冲浪**：如果对 `decoder.h` 里面某个稀奇古怪的首字母缩写（例如 `pcaddu12i`）不明白，建议在网上搜一搜 *LoongArch 龙芯指令集体系结构手册* 配合着读，你会惊讶地发现代码里的处理和实体硬件文档的描述如出一辙。

祝你阅读愉快，征服这台属于你的模拟器！✨
